apiVersion: v1
kind: ConfigMap
metadata:
  name: app-cm
  namespace: {{ .Release.Namespace }}
data:
{{- range $key, $val := .Values.configMaps.appConfig }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
