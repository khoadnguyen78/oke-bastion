apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "typescript-docker-multi-stage-build.fullname" . }}-test-connection"
  labels:
    {{- include "typescript-docker-multi-stage-build.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "typescript-docker-multi-stage-build.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
